import { CampaignsClient } from "@/components/campaigns/CampaignsClient";
export const metadata = { title: "Campaigns · ForgeEC" };
export default function CampaignsPage() {
  return <CampaignsClient />;
}
